<van-cell-group title="请选择日期">
  <van-field
    value="{{ currentDateStr }}"
    label="预约时间"
    icon="calender-o"
    bind:tap="onDateClick"
    size="large"
    disabled
  />
</van-cell-group>

<van-cell-group title="请选择时间段" class="order-list">
  <van-cell wx:for="{{orders}}" wx:key="id" wx:for-index="index" wx:for-item="item"     bind:click="onClick" 
    data-id="{{item.id}}"
    data-start="{{item.start}}"
    data-end="{{item.end}}"
    >
    <view slot="title">{{item.start}} - {{item.end}}</view>
    <block wx:for="{{item.room}}" wx:key="id" wx:for-index="idx" wx:for-item="itm">
      <span slot="right-icon">房间{{idx + 1}}: </span>
      <block wx:for="{{itm.seats}}" wx:key="id" wx:for-index="idx2" wx:for-item="itm2">
        <!--<image style="width:48rpx;height:48rpx;" class="m-r-8" wx:if="{{itm2.hasPeople}}" src="{{itm2.sex === 1 ? '../../images/male.png' : '../../images/female.png'}}" /> -->
        <van-icon wx:if="{{itm2.hasPeople}}" class="m-r-8 f-s-20" slot="right-icon" name="manager" 
          color="{{itm2.sex === 1 ? 'blue' : 'red'}}" ></van-icon>
        <van-icon wx:if="{{!itm2.hasPeople}}" slot="right-icon" class="m-r-8 f-s-20" name="manager-o" />
      </block>
    </block>
  </van-cell>
</van-cell-group>

<van-action-sheet show="{{ show }}" bind:close="onClose">
  <van-datetime-picker
    type="date"
    value="{{ currentDate }}"
    min-date="{{ minDate }}"
    max-date="{{ maxDate }}"
    formatter="{{ formatter }}"
    bind:cancel="onClose"
    bind:confirm="onDateConfirm"
  />
</van-action-sheet>
<van-button 
  wx:if="{{isAdmin}}"
  round
  icon="setting-o" type="primary" 
  class="float-button"
  bind:tap="onSettingClick"
  >
</van-button>
<van-action-sheet show="{{ showSetting }}" bind:close="onSettingClose">
  <van-picker
    columns="{{ columns }}"
    default-index="{{ 0 }}"
    show-toolbar="true"
    title="管理员权限"
    bind:confirm="onSettingConfirm"
    bind:cancel="onSettingClose"
  />
</van-action-sheet>

<van-toast id="van-toast" />
